spring:
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:used_car_db_test
    username: dbuser
    password: dbpassword
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
